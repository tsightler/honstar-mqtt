# Acura EV Connect - MQTT Gateway Configuration

# Acura account credentials (required)
ACURA_USERNAME=your@email.com
ACURA_PASSWORD=yourpassword

# Vehicle VIN (optional, uses first vehicle if not specified)
# ACURA_VIN=YOUR_VIN_HERE

# MQTT broker URL (required)
# Examples:
#   mqtt://192.168.1.100:1883           (no auth)
#   mqtt://user:pass@192.168.1.100:1883 (with auth)
#   mqtts://user:pass@broker.example.com:8883 (TLS)
MQTT_URL=mqtt://user:pass@localhost:1883

# Poll interval in seconds (default: 900 = 15 minutes)
# POLL_INTERVAL=900

# Enable debug logging (default: false)
# DEBUG=false
