{
  "name": "acura-ev-mqtt",
  "version": "1.2.0",
  "description": "MQTT gateway for Acura EV connected vehicle services — polls vehicle status and publishes to MQTT",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "mqtt": "^5.10.0",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
